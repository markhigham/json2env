{
  "name": "@markhigham/json2env",
  "version": "0.0.1",
  "description": "converts json on input to .env/.ini format",
  "bin": "bin/json2env.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier bin --write"
  },
  "keywords": [],
  "author": "Mark Higham",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^3.0.0"
  }
}
